{
  "buildCommand": "cd .. && npm run build && cd ejemplo && npm run build:remotes && npm run build -w packages/shell && node scripts/merge-dist.mjs",
  "outputDirectory": "packages/shell/dist",
  "installCommand": "rm -f package-lock.json && cd .. && npm install && cd ejemplo && npm install"
}
