import { Routes } from '@angular/router';
import { NavigationWindowComponent } from './features/navigation-window/navigation-window.component';

export const routes: Routes = [
  { path: 'navigation-window', component: NavigationWindowComponent },
];
